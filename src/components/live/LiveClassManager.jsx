import ComingSoon from '../common/ComingSoon';

const LiveClassManager = () => {
  return <ComingSoon feature="Live Classes" />;
};

export default LiveClassManager;

/* ========================================
   LIVE CLASS FEATURE - TEMPORARILY DISABLED
   ========================================
   
   This feature is under development and will be available soon.
   Original code has been backed up to LiveClassManager.jsx.backup
   
   To re-enable:
   1. Restore code from backup file
   2. Ensure backend live class routes are enabled
   3. Test Jitsi integration
   4. Update documentation
   
   ======================================== */
